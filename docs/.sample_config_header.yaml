# This file is maintained as an up-to-date snapshot of the default
# homeserver.yaml configuration generated by Relapse. You can find a
# complete accounting of possible configuration options at
# https://clokep.github.io/relapse/latest/usage/configuration/config_documentation.html
#
# It is *not* intended to be copied and used as the basis for a real
# homeserver.yaml. Instead, if you are starting from scratch, please generate
# a fresh config using Relapse by following the instructions in
# https://clokep.github.io/relapse/latest/setup/installation.html.
#
################################################################################

